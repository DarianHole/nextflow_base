#!/usr/bin/env nextflow

// enable dsl2 if you want, isn't needed to do so
// nextflow.preview.dsl = 2

// import subworkflows if there are any
// include {<subworkflow_name>} from './workflows/<file_name>.nf'

// Parameter checking
/*

if ( !params.input ) {
    println("Please specify an input")
    System.exit(1)
}

*/

// Main Workflow
/*

workflow {
    do your workflow, add channels, etc!
}

*/